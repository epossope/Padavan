# Noema Mini App UI Polish V1.2

Deterministic local proof at a 390 × 844 viewport.

- `before-*.png` — UI from commit `32d2b40`, captured before V1.2.
- `after-*.png` — UI after the V1.2 compactness pass.
- `comparison-*.png` — side-by-side `BEFORE | AFTER` proof.

The screenshots use synthetic local fixture data. No Telegram session, messages, audio, personal data, provider calls, or secrets are used.
