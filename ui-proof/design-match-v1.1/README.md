# Noema Design Match V1.1 — density proof

Each comparison uses the supplied immutable reference, the c5fca87 Design Match V1 screenshot, and the V1.1 result at a 390 × 844 viewport.

- comparison-*.png — side-by-side proof: REFERENCE | CURRENT c5fca87 | V1.1.
- reference-*.png — copies of the supplied source-of-truth screens.
- current-c5fca87-*.png — the unchanged V1 baseline.
- v1.1-*.png — deterministic screenshots of the compactness pass.

The fixture is synthetic and local-only. It does not connect to Telegram, call provider APIs, or persist user data.
